# --- Core web framework ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# --- Database ---
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9

# --- Validation / settings ---
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# --- Video pipeline ---
yt-dlp>=2024.1.0
faster-whisper>=1.0.0
openai>=1.30.0
httpx>=0.26.0
# NOTE: ffmpeg is a system binary (not a pip package). It must be installed
# via the OS package manager or bundled in the Docker image (see
# skills/DEPLOYMENT.md). FFMPEG_PATH in app/config.py points to the binary.

# --- Dev / quality / testing ---
ruff>=0.4.0
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0
